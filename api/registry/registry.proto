syntax = "proto3";
package kratospkg.api.registry;

option go_package = "github.com/im-gc/kratos-pkg/api/registry;registry";
option objc_class_prefix = "Registry";

message Registry {
  bool enabled = 1;
  repeated string endpoints = 2;
}
